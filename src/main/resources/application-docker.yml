spring:
  data:
    redis:
      standalone:
        host: redis
        port: 6379

  datasource:
    url: jdbc:mysql://mysql:3306/study?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: skdus4972@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        connection:
          characterEncoding: UTF-8
          useUnicode: true
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  # RabbitMQ 설정 (이벤트 발행용)
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: admin
    password: admin123
    virtual-host: /
    listener:
      simple:
        auto-startup: false  # RabbitMQ가 없어도 애플리케이션이 시작되도록 설정
        retry:
          enabled: false

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

# 로깅 설정
logging:
  level:
    com.study_group_service: DEBUG
    org.springframework.amqp: DEBUG
