spring:
  data:
    redis:
      standalone:
        host: localhost
        port: 6379

  datasource:
    url: jdbc:mysql://localhost:3306/StudyService?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: skdus4972@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
